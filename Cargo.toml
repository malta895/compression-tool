[package]
name = "compression-tool"
version = "1.0.0"
edition = "2021"
description = "The fastest and most efficient Compression Tool - Free if you can click a button"

[dependencies]
clap = { version = "4.5.17", features = ["derive"] }
